#pragma once

// Configuration for vertical 800x480 display (e.g., Raspberry Pi with vertical screen)
// Each screen (Top/Bottom) takes half the height
const int SCREEN_SIZE_X = 800;
const int SCREEN_SIZE_Y = 240;  // 480 total / 2 screens