#pragma once

// Configuration for vertical 480x800 display (portrait mode)
// Each screen (Top/Bottom) takes half the height
const int SCREEN_SIZE_X = 480;
const int SCREEN_SIZE_Y = 400;  // 800 total / 2 screens